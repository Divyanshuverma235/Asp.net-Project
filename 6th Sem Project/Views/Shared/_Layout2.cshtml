<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CodeSavvy</title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Overall body styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #fff;
        }

        /* Navbar Styling */
        .navbar {
            background-color: #f9f6e6;
            padding: 10px 20px;
        }

        /* Sidebar styling */
        .sidebar {
            width: 250px;
            background-color: #f9f6e6;
            height: 100vh; /* Full height */
            position: fixed;
            padding: 20px;
            top: 56px; /* Navbar ke neeche shift */
        }

            /* Sidebar links */
            .sidebar a {
                display: block;
                padding: 10px;
                text-decoration: none;
                color: black;
                margin-bottom: 5px;
            }

                /* Active or hovered sidebar links */
                .sidebar a.active, .sidebar a:hover {
                    background-color: gold;
                    border-radius: 5px;
                }

        /* Main content area (pushed right because of sidebar) */
        .content {
            margin-left: 270px;
            padding: 20px;
            margin-top: 56px; /* Navbar space */
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar navbar-light">
        <span class="navbar-brand mb-0 h4">CodeSavvy</span>
    </nav>

    <!-- Sidebar Navigation Menu -->
    <div class="sidebar">
        <a href="profile.html" class="active">My Profile</a>
        <a href="enrolled-courses.html">Enrolled Courses</a>
        <a href="wishlist.html">Wishlist</a>
        <a href="settings.html">Settings</a>
        <a href="logout.html">Log Out</a>
    </div>

    <!-- Main Content (Yahan Profile Page Ka Content Load Hoga) -->
    <div class="content">
        {% block content %} {% endblock %}
    </div>

</body>
</html>
